"""CloudGuard - a multi-cloud IAM misconfiguration scanner."""

# Single source of truth for the version. setup.py reads this, and --version on
# the CLI prints it, so there's only ever one place to bump.
__version__ = "0.1.0"
